<div class="row">
    <div class="col-md-3">
    </div>
        <div class="col-md-7">
				<p>本当に退会しますか？</p>
				<p>退会すると、会員登録情報やこれまでの購入履歴が閲覧できなくなります。</p>
			    <p>退会する場合は、「退会する」をクリックしてください。</p>
			    <ul class="nav nav-pills">
				<li><%= link_to "退会しない", customer_path(current_customer), class: "btn-sm btn-primary" %></li>
				<li>
				<%= form_for @customer do |f| %>
				<%= f.submit "退会する", class: "btn-sm btn-danger" %>
		        <% end %>
		        </li>
	    </div>
</div>